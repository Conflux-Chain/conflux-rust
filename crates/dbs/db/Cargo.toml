[package]
name = "db"
version = "0.1.0"
edition = "2024"
license-file.workspace = true

[dependencies]
log = { workspace = true }
kvdb-rocksdb = { workspace = true }

[features]
portable = ["kvdb-rocksdb/portable"]
